---
name: SSL 憑證到期監控 (SSL Certificate Expiry Monitor)
description: "Use Case #087: SSL 憑證到期監控 (SSL Certificate Expiry Monitor) - 监控 SSL 证书过期时间，在证书即将过期时发送提醒，避免服务中断。。輸入 /SSL 憑證到期監控 (SSL Certificate Expiry Monitor)/python 或 /SSL 憑證到期監控 (SSL Certificate Expiry Monitor)/nodejs 選擇方案，/SSL 憑證到期監控 (SSL Certificate Expiry Monitor)/compare 比較兩方案。"
---

# Use Case #087: SSL 憑證到期監控 (SSL Certificate Expiry Monitor)

> 編號: 087 / 118 | 分類: 監控與維運 | 難度: 初中級 | 時間: 30-60 分鐘

## 一句話描述

监控 SSL 证书过期时间，在证书即将过期时发送提醒，避免服务中断。

## 可用子指令

| 指令 | 說明 |
|------|------|
| `/SSL 憑證到期監控 (SSL Certificate Expiry Monitor)/python` | Python 完整實作教學 |
| `/SSL 憑證到期監控 (SSL Certificate Expiry Monitor)/nodejs` | Node.js 完整實作教學 |
| `/SSL 憑證到期監控 (SSL Certificate Expiry Monitor)/compare` | 兩方案詳細比較 |

## 快速推薦

- **初學者 / 快速原型** → `/SSL 憑證到期監控 (SSL Certificate Expiry Monitor)/nodejs`
- **正式環境 / 長期穩定** → `/SSL 憑證到期監控 (SSL Certificate Expiry Monitor)/python`

## 功能需求



## 核心技術棧



## 成本估算

| 項目 | 費用 |
|------|------|
| Claude API | ~$0.50-5.00/月 |
| 第三方 API | 視用量（多數有免費額度） |

## 原始參考資料

- `hub`: `61-ssl-certificate-monitor.md`

## 詳細教學文件

- Python 方案: `/SSL 憑證到期監控 (SSL Certificate Expiry Monitor)/python`
- Node.js 方案: `/SSL 憑證到期監控 (SSL Certificate Expiry Monitor)/nodejs`
- 方案比較: `/SSL 憑證到期監控 (SSL Certificate Expiry Monitor)/compare`
